import React from 'react';
import {MyPhoto} from "./_components/homePage.component";

const App= ()=>{
return(
   <div style={{textAlign:'center'}}> 
    <MyPhoto onSubmit={()=>{}} />
    </div>
   );
};

export default App;
